<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.TestMapper">
    <select id="getAll" resultType="data.entity.Test">
        select * from test
    </select>
</mapper>